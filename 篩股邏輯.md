# 美股掃圖 - 篩股邏輯

## 版本摘要

目前篩選邏輯版本：`1.1`

核心變更參照 `篩股邏輯-0604.md`：

| 項目 | 條件 |
| --- | --- |
| 型態優先序 | C > A > B |
| 型態 A 糾結條件 | 最近 10 個交易日每日 MA10/MA20 差距 <= 3% |
| 型態 C 回撤區間 | 最近 65 根 K 線 |
| 型態 C 回撤幅度 | 16%~36% |
| 型態 C 反彈條件 | 反彈 >= 8%，且反彈高點不可高於前高 |
| 型態 C 低點防守 | 回撤低點之後不可再破低 |

## 股票範圍

- 資料來源：Nasdaq screener。
- 僅保留 `country = United States` 的普通股與可交易標的。
- 排除 ADR / ADS、ETF、ETN、基金、權證、Unit、Right、Index 等非普通股類型。
- Yahoo Finance 代號中的 `/` 會轉成 `-`。

## 資料條件

- 價格資料至少需要最近 `1.2` 年。
- 價格來源：Yahoo Finance via `yfinance`。
- 相對強弱比較基準：`SPY`。
- 可用 `MAX_STOCKS` 限制測試更新檔數。

## 型態篩選

個股只要符合型態 A、型態 B、型態 C 任一種，就會進入下一重篩選。

若同一檔股票同時符合多個型態，顯示與輸出型態依下列優先序：

```text
C 優先，其次 A，最後 B
```

## 型態 A：漲後整理

型態 A 使用 A/B 共用基底條件，並額外要求均線糾結。

### A/B 共用基底條件

| 條件 | 說明 |
| --- | --- |
| 資料長度 | 至少 120 根 K 線 |
| 波段結構 | 最新低點高於 120~90 日前高點，且近 30 日低點與 60 日前低點維持墊高 |
| 季線壓制排除 | 近半年低於 MA60 的天數 <= 60 天 |
| MA10/MA20 同站 MA60 比例 | 近半年內有 >= 25% 的交易日，MA10 與 MA20 均在 MA60 之上 |
| 收盤站上季線 | 最新收盤價 > MA60 |

### A 額外條件

最近 10 個交易日，MA10 與 MA20 的差距每天都必須小於等於 3%。

```text
近 10 日每日 abs(MA10 - MA20) / MA20 <= 3%
```

## 型態 B：多頭排列

型態 B 同樣使用 A/B 共用基底條件，並額外要求均線多頭排列。

```text
MA10 > MA20 > MA60
```

## 型態 C：回撤反彈

型態 C 不使用 A/B 的均線基底條件，而是使用近 65 根 K 線的高點、回撤與反彈條件。

### C 條件

| 條件 | 說明 |
| --- | --- |
| 資料長度 | 至少 65 根 K 線 |
| 觀察區間 | 最近 65 根 K 線 |
| 高點位置 | 找出 65 根 K 線內最高價 |
| 高點不可太晚 | 高點後至少要有 2 根 K 線，才有足夠資料確認回撤與反彈 |
| 前段漲幅 | 高點相對高點前最低價需上漲 >= 50% |
| 回撤幅度 | 高點後低點回撤 16%~36% |
| 反彈幅度 | 回撤低點後高點反彈 >= 8% |
| 反彈不過高 | 反彈高點不可高於前高 |
| 低點防守 | 回撤低點之後不可再跌破該低點 |

```text
16% <= (高點 - 回撤低點) / 高點 <= 36%
(回撤後高點 - 回撤低點) / 回撤低點 >= 8%
回撤後高點 <= 前高
回撤低點之後的最低價 >= 回撤低點
```

## 波段篩選

通過型態後，仍需找到符合條件的上升波段：

| 條件 | 說明 |
| --- | --- |
| 最小漲幅 | >= 20% |
| 最小持續天數 | >= 5 個交易日 |
| 波段低點 | 波段起點低點不可跌破前一波段低點 |
| 波段辨識 | 使用局部高低點，視窗為前後 15 根 K 線 |

## 成交量條件

通過型態與波段後，仍需符合成交量條件。

```text
近 10 日均量 >= 500,000 股
```

## 標注（不篩選，僅顯示）

| 標注 | 條件 |
| --- | --- |
| `RS↑` | 近 60 日相對 SPY 強勢 |
| `VOL↑` | 近 5 日均量 >= 近 20 日均量的 1.6 倍 |
| `Theme↑` | USStockFlow 主題強度大於 0 |

## 族群與產業

- `sector` / `industry` 來自 Nasdaq screener。
- 領漲族群與領跌族群使用 `stock_concepts.json` 的全市場主題資料，不使用目前型態或產業篩選後的股票清單重算。
- 顯示的 `篩出 N 檔` 代表該主題在全市場涵蓋的股票數，不代表目前頁面篩選結果中的檔數。

## 程式對應

| 項目 | 程式位置 |
| --- | --- |
| A/B 共用基底 | `update_data.py` 的 `_check_base()` |
| 型態 A | `update_data.py` 的 `check_type_a()` |
| 型態 B | `update_data.py` 的 `check_type_b()` |
| 型態 C | `update_data.py` 的 `check_type_c_pullback_rebound()` |
| 波段篩選 | `update_data.py` 的 `identify_uptrend()` |
| 成交量篩選 | `update_data.py` 的 `check_volume()` |
| 前端型態篩選與顯示 | `app.py` |
| 主題資料產生 | `update_data.py` |
| 主題資料檔案 | `stock_concepts.json` |
